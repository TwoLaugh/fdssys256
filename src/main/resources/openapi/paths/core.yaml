decisionById:
  get:
    tags: [DecisionLog]
    operationId: getDecisionLogEntry
    summary: Get a single decision-log entry by ID.
    security:
      - cookieAuth: [admin]
    parameters:
      - in: path
        name: decisionId
        required: true
        schema: { type: string, format: uuid }
    responses:
      '200':
        description: The decision-log entry.
        content:
          application/json:
            schema: { $ref: '../schemas/core.yaml#/DecisionLogDto' }
      '404':
        description: Resource not found.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
      '401':
        description: Missing or invalid authentication.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
decisionsByTrace:
  get:
    tags: [DecisionLog]
    operationId: getDecisionLogByTrace
    summary: Get all decision-log entries for a trace, ordered by creation time.
    security:
      - cookieAuth: [admin]
    parameters:
      - in: path
        name: traceId
        required: true
        schema: { type: string, format: uuid }
    responses:
      '200':
        description: Decisions in the trace; empty list if none exist.
        content:
          application/json:
            schema:
              type: array
              items: { $ref: '../schemas/core.yaml#/DecisionLogDto' }
      '401':
        description: Missing or invalid authentication.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
adminStatus:
  get:
    tags: [AdminStatus]
    operationId: getAdminStatus
    summary: 'Operational status: DB connectivity, last AI/USDA call, month-to-date AI cost.'
    description: >-
      Admin-only operational snapshot (capability C-G-032). status=UP when the database is
      reachable, DEGRADED otherwise. lastAiCallAt / lastUsdaCallAt are null when no such call has
      occurred; lastUsdaCallAt is a process-local liveness signal that resets on restart.
    security:
      - cookieAuth: [admin]
    responses:
      '200':
        description: Current operational status snapshot.
        content:
          application/json:
            schema: { $ref: '../schemas/core.yaml#/AdminStatusDto' }
      '401':
        description: Missing or invalid authentication.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
      '403':
        description: Authenticated but not an admin.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
decisionAncestry:
  get:
    tags: [DecisionLog]
    operationId: getDecisionAncestry
    summary: Walk parent_decision_id recursively up to maxDepth.
    security:
      - cookieAuth: [admin]
    parameters:
      - in: path
        name: decisionId
        required: true
        schema: { type: string, format: uuid }
      - in: query
        name: maxDepth
        schema: { type: integer, minimum: 1, maximum: 32, default: 32 }
    responses:
      '200':
        description: Ancestor chain ordered root-first; cycleDetected is true if the depth cap was hit.
        content:
          application/json:
            schema: { $ref: '../schemas/core.yaml#/AncestryResponse' }
      '400':
        description: Validation or syntactic error.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
      '404':
        description: Resource not found.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
      '401':
        description: Missing or invalid authentication.
        content:
          application/problem+json:
            schema: { $ref: '../schemas/common.yaml#/ProblemDetail' }
