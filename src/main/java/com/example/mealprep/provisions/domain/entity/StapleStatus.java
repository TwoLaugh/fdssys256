package com.example.mealprep.provisions.domain.entity;

/** Discrete stock state for status-tracked items (typically staples on the spice rack). */
public enum StapleStatus {
  STOCKED,
  LOW,
  OUT
}
