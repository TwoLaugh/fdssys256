package com.example.mealprep.feedback.api.dto;

/**
 * Origin screen of a feedback submission. Module-local — restated rather than imported from any
 * cross-module enum (per lld/feedback.md line 269). Values verbatim from lld/feedback.md §DTOs line
 * 250.
 */
public enum Screen {
  RECIPE_DETAIL,
  PLAN_MEAL_DETAIL,
  PLAN_VIEW,
  GROCERY,
  NUTRITION_DASHBOARD,
  SETTINGS,
  GENERAL
}
