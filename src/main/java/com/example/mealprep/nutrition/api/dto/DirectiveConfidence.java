package com.example.mealprep.nutrition.api.dto;

/** Strength of the evidence summary carried with a directive. Optional; nullable in storage. */
public enum DirectiveConfidence {
  LOW,
  MODERATE,
  HIGH
}
